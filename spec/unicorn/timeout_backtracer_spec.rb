require 'spec_helper'

describe Unicorn::TimeoutBacktracer do
  it 'has a version number' do
    expect(Unicorn::TimeoutBacktracer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
